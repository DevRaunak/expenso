import 'package:flutter/material.dart';

class FragStatsPage extends StatelessWidget {
  const FragStatsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.brown.shade50,
    );
  }
}
